```text
Help me add Supabase to my Astro project. Create a Supabase project at
database.new and run the instruments table SQL. Then:
1. Run `npm create astro@latest my-app` to scaffold the app.
2. Install `@supabase/supabase-js` and `@astrojs/node` with `npm install`.
3. Update `astro.config.mjs` to enable SSR with the Node adapter.
4. Create `.env.local` and set `PUBLIC_SUPABASE_URL` and `PUBLIC_SUPABASE_PUBLISHABLE_KEY`.
5. Create `src/lib/supabase.ts` with a `createServerClient()` helper function.
6. Create `src/pages/instruments.astro` to query and display the instruments table.
7. Run `npm run dev` and open http://localhost:4321/instruments.
```
